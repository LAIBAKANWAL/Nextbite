{
  "name": "nextbite",
  "version": "1.0.0",
  "description": "Food delivery app",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.3",
    "jsonwebtoken": "^9.0.0",
    "mongodb": "^5.7.0",
    "mongoose": "^7.5.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0"
  }
}